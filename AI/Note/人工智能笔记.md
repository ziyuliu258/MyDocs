## 重点索引
- 知识工程
	- [逻辑表示法的重要结论](#重要结论)
- 神经网络
	- 
	

## 绪论
### 研究途径
1. 符号主义：认为人工智能源于**数理逻辑**
	应用流派：**知识工程/专家系统**
2. 连接主义：**模拟人脑神经元**
	典型应用：**深度学习**
3. 行为主义：人工智能基于**感知和行动**
	典型应用：**机器人、自动驾驶**
## 知识工程
### 概述
#### 知识定义
将有关信息关联在一起形成的知识结构称为知识。
>雪是白色的 $\to$ *事实*
>如果头痛流涕$\implies$可能感冒了 $\to$ *规则*
#### 知识的特性
- 相对正确性
- 不确定性
	- 随机性（*“可能感冒了“*）
	- 模糊性（*”高个子*适合打篮球”）
	- 不完全性
	- 经验性
- 可表示性和可利用性
#### 知识的分类
- 按照作用范围
	- 常识性
	- 领域性
- 按照作用/表示
	- 事实
	- 规则：**反映因果关系**
- **按确定性**：
	- 确定性知识：可以指出其真值为真/假的；
	- 不确定性知识：对不精确、不完全、及模糊性知识的总称。
### 知识的表示方法
- 知识的两大类表示方法分别是 **符号表示法** 和 **连接机制表示法**。
- 常用的知识表示法：
	- **一阶谓词逻辑表示法**
	- **产生式表示法**
	- **框架表示法**
	- ……
#### 逻辑表示法
- 谓词逻辑：是一种形式语言，也是目前为止最精确的语言。
- 命题的类型：原子命题/复合命题
##### 命题逻辑
最简单的逻辑系统，将命题视为不可分割的最小单位，使用逻辑连接词来构建复合命题。
- 基础逻辑连接词：且（$\land$）、或（$\lor$）、非（$\neg$）、蕴含（$\to$）
- 命题逻辑的局限；
	1. 无法反映事物的**结构/逻辑关系**
	2. 无法反映事物间的**共同关系**
>“李白是诗人”和“杜甫是诗人“，看不出其中关系；
>“每个人要死”，“孔子是人”，“孔子是要死的“，看不出前两个命题的共同特点（孔子也是人）
##### 谓词逻辑
**一般形式**
$$P(x_1, x_2, ..., x_n)$$
- $P$：谓词名
- $x_i$：个体，可以是**常量/变元/函数**；**常量/变元/函数** 统称为*项*
- 个体变元的取值范围称为**个体域**；
- 谓词名是**命题的谓语**，个体是**命题的主语**
$$Friend（A, Brother(B)）\Leftrightarrow A与B的哥哥是朋友$$
- **元数**：谓词中包含的个体数目；
- k阶谓词定义：
	如果在谓词中，$x_i$都是个体变量/变元/函数，则称命题$P$为$1$阶谓词；
	如果在谓词中，$x_i$本身是$k-1$阶谓词，则命题为$k$阶谓词。
**连接词：**
- 非：$¬$；
- 析取：$∨$；
- 合取：$∧$；
- 蕴含：$→$；
- 等价：$⇄$ $⟷$；
![[Pasted image 20251129015614.png]]
>蕴含：只有前提为真且被违背的时候，所得为$F$，其他时候都为真。
>等价：二者相同为真。

**量词：** 全称/存在量词
**谓词公式**
- 合式公式：符合语法规则的公式
- 单个谓词：是合式公式，称为**原子公式**；
- $A$是合式公式$\to \neg A$也是合式公式；
- $A,B$是合式公式$\to$，二者关于上述连接词的组合而成的公式也是合式公式；
- 若𝐴是合式公式，𝑥是𝐴中出现的任一个体变元，则$(∀𝑥)𝐴$, $(∃𝑥)𝐴$都是合式公式。
- **辖域**：位于**量词**后面的单个**谓词**/括弧扩起来的**合式公式**
	- 约束变元：辖域内和量词中变元同名的变元；
	- 自由变元：与上条件相反。
- **解释**：对各命题变元进行**真值指派**称为对命题公式的一个解释
>也就是对逻辑变量赋值（T或者F）。
- 重言式：对于命题变元的所有真值指派，$A$的真值均为$T$，称$A$为重言式/永真式。
- 例题：
	![[Pasted image 20251130164452.png]]
	![[Pasted image 20251130171040.png]]
>这个解释可以随便定，可能理解会有偏差的地方在对于这个公式的理解。这里正确的理解应该是，“**当前解释能否使后面这个语句的值可以为真，如果可以，$A$为真；否则（也就是不存在能让这个语句为真的变量取值）就是假**”。所以就可以解释为什么下图是F，因为不存在能够让语句一直为真的取值组合。

###### 谓词公式的特性
1. 永真
	如果一个谓词公式$P$在个体域上$D$任何一个解释都得到真值$T$，那么称P在D上永真；如果P在每个非空个体域上都永真，那么就称P永真。
2. 可满足/相容性
	如果至少存在一个解释使得P在这个解释下真值是T，那么称P是可以满足的。（类比$\exists$）
3. 永假/不可满足/不相容性
4. 如果$P \to Q$ 永真，则称$P$**永真蕴含**$Q$，称后者是前者的逻辑结论，前者为后者的前提，记为$P\Rightarrow Q$。

##### 重要结论
###### 连接词化归律
$P\to Q \Leftrightarrow \neg P \lor Q$ 
$P\leftrightarrow Q \Leftrightarrow (P\to Q) \land (Q\to P)$

###### 重要的永真蕴含式
- **化简式**
	$$P \land Q \Longrightarrow P, P \land Q \Longrightarrow Q
	$$
- **附加式**
	$$𝑃 ⟹ 𝑃 ∨ 𝑄, 𝑄 ⟹ 𝑃 ∨ Q
	$$
- **析取三段论**
	$$\neg P, P \lor Q \Longrightarrow Q
	$$
>凶手就在两个人之间，如果你不是，那么他一定是！
- **假言推理**
	$$P, P\to Q \Longrightarrow Q
	$$
- **拒取式**
	$$\neg Q, P \to Q \Longrightarrow \neg P
	$$
>可以理解为**反证法**的逻辑！假设结论为假，经过证明推出前提为假，由此证得从前提到结论的推出逻辑成立。
- **假言三段论**
	$$P \to Q, Q \to R \Longrightarrow P\to R
	$$
>经典层层递进三段论逻辑。
- **二难推论**
	$$P \lor Q, P \to R, Q\to R \Longrightarrow R
	$$
>如果A会造成C，B也会造成C，那么只要二者至少一件事发生，就会造成C发生。
- **全称固化**
	$$(\forall x)P(x) \Rightarrow P(y) 
	$$
- **存在固化**
	$$(\exists x)P(x) \Rightarrow P(y)
	$$
>这里这两个“固化”就是消量词的意思。

>*证明永真蕴含式的方法？*
>- 真值表
>- 假设前件为真，推出后件为真
>- 假设前件为假，推出后件为假
##### 推理规则
![[Pasted image 20251206023604.png]]
##### 基于谓词逻辑表示知识
- 事实
	用$\land, \lor$形表示；
- 规则
	用蕴涵式表示（$\to$）
>例子：
>- *人人爱劳动*
>$$(\forall x)Man(x)\to Love(x, Labour)$$
##### 一阶谓词逻辑表示法的特点
- 优点：自然性、精确性、严密性、易于实现
- **缺点：不能表示不确定性/启发性/元知识，组合爆炸，效率低**

---
#### 产生式表示法
##### 基本形式
![[Pasted image 20251206030854.png]]
>捉虫：图中最下例子有错别字

##### 与谓词逻辑表示法的主要区别
产生式的前提条件的匹配可以精确，也可以非精确，更加灵活。

##### 系统组成三部分
- 规则库
- 综合数据库
- 控制系统
![[Pasted image 20251206031510.png]]

##### 规则库
- 是产生式系统的核心；
- 形式：“如果……那么……”（IF-THEN）
- 示例：
	![[Pasted image 20251212133954.png]]
##### 综合数据库/事实库/上下文/黑板（斜杠用以分隔别名）
存放<u>已知的事实</u>/<u>推导出的事实</u>；
##### 控制机制/推理机构
负责整个产生式系统的运行。使用规则对数据库进行推理，以推导出新的事实或者执行动作。
负责工作列举：
- **匹配**：按照一定策略匹配规则的条件部分；
- **冲突消解**：多于一条的规则匹配成功，就选择其中一条执行；
- **更新**：更新数据库/执行相应操作
- **计算**：计算结论的不确定性
- **设置终止条件**：决定系统何时终止运行
##### 一般求解步骤（正向）
![[Pasted image 20251212144808.png]]
##### 分类
- 按照推理方向
	- 正向（数据驱动）
	- 后向（目标驱动）
	- 双向
- 按确定性
	- 确定性产生式系统
	- 不确定性产生式系统
![[Pasted image 20251212145033.png]]
##### 例题
![[Pasted image 20251212145721.png]]

> **解题步骤（正向）**
> 1. 建立事实库（Working Memory, WM）：
 >`WM = { 有毛, 吃草, 黑条纹 }`
 >2. 检查是否存在可用未用的规则，并匹配前提：
 >	如图，检验出标红的4条；只有R1匹配成功；
 >3. 执行规则并将结论加入事实库（更新）。
 >	现在知识库为`WM = { 有毛, 吃草, 哺乳类, 黑条纹 }`
 >4. 检查事实库是否包含新结论，是，故回到第2步；
 >5. 匹配前提，发现R4可用，于是继续对R4重复步骤3和步骤4；
 >6. 现在知识库为`WM = { 有毛, 吃草, 黑条纹, 哺乳类, 有蹄类 }`，返回步骤2；
 >7. 匹配前提，发现R8可用，重复步骤3和步骤4，回到步骤2；
 >8. 检查所有可用规则已用，要求用户增添事实；
 >9. 不需要更多事实，推理终止，结论为**斑马**

##### 特点
- 优点
	![[Pasted image 20251212153918.png]]
- 缺点
	- 效率不高：反复执行“匹配——冲突消解——执行”过程，容易引起组合爆炸。
	- 不能表达具有结构性的知识，不能把有结构关系的事物之间的区别和联系表示出来。
- 适用领域
	![[Pasted image 20251212154725.png]]
#### 层次结构表示法

---
## 搜索
### 搜索方法分类
- 盲目搜索
- 启发式搜索
### 问题表示方法
#### 状态空间表示法
$状态+算符\to 问题$
##### 状态
一般用一个向量表示；
##### 算符
使得问题从一个状态转变到另一个状态的操作称为算符。
在产生式系统中，**一条产生式规则就是一个算符**。
##### 状态空间
$状态空间=所有可能出现的状态+一切可用算符$

在这种表示方法下，解题的关键在于
- 用状态空间把问题的一切状态都表示出来；
- 定义一组算符
- 问题的解就是从初始状态到目标状态所采用算符的序列
- 最优解就是使用算符最少的解
#### 与/或树表示法
##### 定义
- 与树：把一个问题分成$n$个子问题，这$n$个子问题必须同时被满足（都必须解决）；划分与树的过程称为**分解**；
- 或树：$n$个子问题有之一被解决就可以；划分或树的过程称为**等价变换**；
- 图示：
	![[Pasted image 20251209010049.png]]
##### 基本概念
- 本原问题：不能再分解/变换，直接可解的；
- 端节点：没有子节点的节点；
- 终止节点：本原问题对应的节点；
- 可解节点：
	- 满足下列条件之一的：
		- 是一个终止节点；
		- 是一个或节点，且子节点中至少有一个可解；
		- 是一个与节点，且子节点全部可解；
- 不可解节点：三个条件都不满足的。
- 解树：
	![[Pasted image 20251209022716.png]]
### 搜索策略
#### 大纲
![[Pasted image 20251209023001.png]]
#### 搜索策略对比
##### 盲目搜索
不使用启发性信息，按照规定路线规则进行，适用于**状态空间是树状结构/一般图结构的问题**。
代表方法：DFS、BFS及其各种变种。
##### 启发式搜索
使用启发式信息，可以高效求解结构复杂的问题。
代表方法：A\*算法等。
#### OPEN/CLOSE表
- 搜索算法基本流程
![[Pasted image 20251209023841.png]]
- OPEN/CLOSE表介绍
![[Pasted image 20251209023936.png]]
#### （==重要==）状态空间一般搜索过程
![[Pasted image 20251209024200.png]]
![[Pasted image 20251209024213.png]]
#### 广度优先搜索（BFS）
##### 基本思想
- 逐层拓展，在某一层节点没有全部拓展及考察之前，不对下一层节点拓展。
- OPEN表中按照**FIFO**的逻辑处理。
##### 基本流程
![[Pasted image 20251211234048.png]]
##### 示例：重排九宫
![[Pasted image 20251211234348.png]]
注意看以下图示的具体过程

![[Pasted image 20251212015834.png]]
![[Pasted image 20251212015846.png]]
##### 注意事项
1. 扩展节点一定要按固定的**顺序**来使用操作符，比如上例中，一直是 “*左——上——右——下*“ 的顺序。
2. 扩展的时候，碰见子节点前面出现过，就直接丢弃；
3.  BFS的本质是：初始节点是根结点，在状态空间图中按照BFS的原则生成一颗搜索树。
##### 特点
- 优点
	完备性：只要有解，BFS一定能得到，且能得到最短路径的解；
- 缺点
	盲目性大，如果目标离初始节点远，搜索效率低；
#### 深度优先搜索（DFS）
##### 基本思想
- 每次拓展后，如果没有得到目标节点，就选择<u>最后产生的子节点</u>拓展；直到某一时刻到达一个子节点，且它既不是目标节点也不能继续拓展，则选择它的兄弟节点考察。
- 概括而言，OPEN表里是**LIFO**，后进先处理。
><u>重点区别记忆</u>：
>**BFS *vs* DFS**
>- BFS把子节点放到OPEN表**尾部**；
>- DFS把子节点放进OPEN表**首部**
##### 基本流程
![[Pasted image 20251212030239.png]]
>注意放入顺序。
##### 示例：重排九宫
![[Pasted image 20251212025358.png]]
![[Pasted image 20251212030117.png]]
![[Pasted image 20251212030346.png]]

##### 特点
- 不完备性
	如果目标节点不在这个分支，且这个分支是无穷分支，那么就不可能得到解；
- 求得可行的解**不一定是路径最短的解**；
- 本质：按照DFS原则生成搜索树。
#### 有界深度优先搜索
##### 基本思想
引入搜索深度界限$d_m$，来解决无限搜索某一分支的问题。
##### 基本流程
![[Pasted image 20251212030903.png]]
![[Pasted image 20251212030914.png]]
##### 困难与改进方法
- 困难
	- 深度界限具体值很难确定，且它还会实际地影响到能否求得解；
	- 同样也不保证最优解。
- 改进方法
	1. 确定界限值：动态设置
		![[Pasted image 20251212031242.png]]
	2. 找到最优解
		![[Pasted image 20251212031346.png]]
		>可以理解为，用当前解集合不断收紧解路径长度的约束。
#### 代价树的广度优先搜索
##### 基本定义
![[Pasted image 20251212031610.png]]
>可以如此概括：
>- 代价树就是带权树；
>- 上面这个代价表达式是典型的动态规划
##### 基本思想
![[Pasted image 20251212213521.png]]
>- OPEN表类似所谓**单调栈**，就是队列排序。
>- 这里可以看成是一种贪心算法，或者也可以说是Dijkstra算法。

##### 代价树示例
![[Pasted image 20251212214508.png]]
##### 算法流程
![[Pasted image 20251212213938.png]]

##### 特点
- 完备：如果有解，代价树的BFS一定可以求得解，并且求出的是最优解。

#### 代价树的深度优先搜索
##### 核心
相比代价树BFS，主要区别在于：***本算法是对刚拓展出的节点按照代价从小到大排序，并放进OPEN表中；而代价树的BFS是将拓展出的节点先放进OPEN表，然后对全体OPEN表中节点进行排序***。

##### 过程
![[Pasted image 20251212215153.png]]

##### 特点
不完备，理由同普通DFS。

#### 启发式搜索
##### 估价函数
用于评估节点重要性的函数。
$$f(x)=g(x)+h(x)
$$
- $g(x)$：初始节点到节点x的**代价**；有利于搜索完备性，但是影响搜索的效率；
- $h(x)$：启发函数，是从x到目标节点最优路径**代价的估计**，是一种启发式信息；有利于提高搜索效率，但是影响搜索完备性。
#### 局部择优搜索（对比深度优先搜索）
##### 算法基本思想
![[Pasted image 20251212225744.png]]

##### 与盲目搜索算法的联系
![[Pasted image 20251212225858.png]]
两种算法都是局部择优搜索的特例。
#### 全局择优搜索（对比广度优先搜索）

##### 算法基本思想
![[Pasted image 20251212230019.png]]

##### 与盲目搜索算法的联系
![[Pasted image 20251212230526.png]]
##### 例题
![[Pasted image 20251212231535.png]]

#### A\*算法
##### 基本思想
![[Pasted image 20251212232314.png]]
>$h^*(x)$是启发函数的理想上界。
##### 启发函数
![[Pasted image 20251212232243.png]]


---
## 神经网络
### 多层感知机 MLP
#### MP模型
MP 模型（McCulloch–Pitts Model，又称 **M-P 神经元模型**）是最早的**人工神经元模型**之一，由 McCulloch 和 Pitts 在 1943 年提出。它奠定了现代人工神经网络的基础。
设输入为
$$
I_1, I_2, ..., I_n
$$
权重为
$$
w_1, w_2, ..., w_n
$$
偏置为 b。

$$
S = \sum_{i=1}^n w_i I_i + b
$$
$$
f(S) = 
\begin{cases}
1, & S > \theta \\
0, & S \le \theta
\end{cases}
$$

或者写成：
$$
f(S) = \text{sign}(S)
$$
和感知机的关系？
***感知机 = 可学习的MP模型***；

#### 感知机
单层感知器（Perceptron）就是多个M-P模型的堆叠（多个神经元），其引入学习的概念，即通过**梯度下降法**对**损失函数**极小化。
##### 缺陷
==**不能解决线性不可分问题！**==
>线性不可分问题：无法用一个平面(直线)把超空间(二维平面)中的点正确划分为两部分的问题。

在输入和输出层间加上一层或多层的神经元(隐层神经元)，就可构成多层前向网络，这里称为多层感知器。
在不使用**反向传播（BP）** 的前提下，只有多层感知机输出层和最后一层隐藏层之间的权重是可训练的。
![[Pasted image 20251208153819.png]]
多层感知机属于**前馈神经网络**的一种。

### 前馈神经网络
#### 几个定理
- 万能逼近定理
	![[Pasted image 20251208154058.png]]
	>也就是说，一个FNN在不考虑成本的情况下，可以逼近任意一个函数。
- 神经标度律（Scaling Laws）
	![[Pasted image 20251208154445.png]]
	>参数/数据量越大，相对应获得的提高就越慢（可理解成某种边际效应）
	>这里这个$N$可以指
	>- 模型规模
	>- 数据集规模
	>- 训练用的计算资源

#### 前向传播
![[Pasted image 20251208154926.png]]
![[Pasted image 20251208155021.png]]

#### 训练方法
一个BP前馈神经网络的基本的训练步骤可以被总结如下：
1. 划分数据集，参数初始化；
2. 前向传播（FP）
	输出层生成的预测结果和标签之类的差异，通过**损失函数（如MSE/MAE/Cross-Entropy Loss等）** 来衡量；
3. 反向传播（BP）
	计算损失函数关于输出层的梯度，利用链式法则，逆向逐层计算损失函数关于每个权重/偏置的梯度；
4. 参数更新
	更新方法：梯度下降/其他优化器（常用：Adam、RMSprop等）。标准形式如下：
	$$\theta^{(t+1)} = \theta^{(t)} - \eta \, \nabla_\theta L(\theta^{(t)}) 
	$$
#### 关于训练参数
##### 学习率
学习率$\eta$如果过小，可能导致收敛速度慢/陷入局部最优；如果过大，可能导致算法不收敛（震荡）；
##### 损失函数最小化
梯度下降方法不能保证获得最小值，只能保证得到极小值。
怎么跳出局部极小？
- **对权系数随机初始化**
- 模拟退火
>模拟退火在每一步都以一定的概率接受比当前解更差的结果，从而有助于“跳出”局部极小。在每步迭代过程中，接受“次优解”的概率要随着时间的推移而逐渐降低，从而保证算法稳定。
- 遗传算法
- 随机扰动
>使用**随机梯度下降**，在计算梯度的时候加入随机因素，在陷入局部最小点的时候计算出的梯度可能仍不为0，这样有机会跳出局部极小。

##### 停止训练
![[Pasted image 20251208161357.png]]
在过拟合时及时利用Early-Stopping结束训练。

##### 过拟合的缓解策略
- Early Stopping
- 正则化 Regularization
![[Pasted image 20251208161659.png]]
>过拟合的出现，一般来说是因为网络在训练集上形成得太复杂，泛化能力差。所以希望模型：
>- 权重不要太大
>- 决策边界不要太弯
>- 模型更“平滑”
>**正则化是在损失函数中加入一项惩罚项，让网络不要学得太复杂，从而防止过拟合。**
>上图是典型的”L2正则化“，也就是常听到的**权重衰减（Weight Decay）**，用在回归任务上是**岭（Ridge）回归**。第二项是正则化项，为了**强迫权重变小**。
>> - 大权重 = 网络变化剧烈 = 模型复杂
>> - 小权重 = 网络输出平滑 = 模型简单、泛化好
>
>而L1正则化相比L2正则化，更易让某些不重要的权重归零，产生**稀疏解**。这块的数学推导没看懂，暂时放一下[链接](https://www.cnblogs.com/zingp/p/10375691.html)




